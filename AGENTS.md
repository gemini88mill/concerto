# Project Rules

- This project uses Bun for tooling and scripts.
- Prefer arrow functions over `function` declarations.
- Prefer `interface` over `type` for object shapes.
- do not use type assertions like 'as'
